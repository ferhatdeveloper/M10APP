# Yerel önizleme: docker compose up --build
# Dokploy: Dockerfile provider, port 80, domain apk.retailex.app (HTTPS proxy'de)
services:
  m10-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped
